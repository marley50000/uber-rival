services:
  - type: web
    name: spark-rides
    env: python
    buildCommand: pip install -r requirements.txt && python app.py
    startCommand: python app.py
    envVars:
      - key: PYTHON_VERSION
        value: 3.9.0
